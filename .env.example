# CloudVuln Frontend Production Environment Variables
# Set to your deployed Render API backend URL (e.g. https://cloudvuln-api.onrender.com/api)
# Or leave empty when using Vercel rewrites to proxy /api directly
VITE_API_URL=https://cloudvuln-api.onrender.com/api
